function renderGetStarted(container: Element): void {
  container.innerHTML = `
  <div id='no-notes-page'>
    <h1>Get started</h1>
    <p>Create a note from the sidebar</p>
  </div>
  `
}

export { renderGetStarted }
